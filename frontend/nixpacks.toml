[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
# As variáveis VITE_* devem estar disponíveis durante o build
# Railway injeta essas variáveis automaticamente do painel Variables
cmds = ["npm run build"]

[start]
cmd = "npm run preview" 