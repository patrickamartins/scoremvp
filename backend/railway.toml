[build]
builder = "nixpacks"

[deploy]
startCommand = "python app/start.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

