# ScoreMVP Monorepo

**Backend**: código Flask em `scoremvp-backend/`  
**Frontend**: app React em `scoremvp-frontend/`  

## Scripts
- `cd scoremvp-backend && pip install -r requirements.txt && flask run`
- `cd scoremvp-frontend && npm install && npm start`
